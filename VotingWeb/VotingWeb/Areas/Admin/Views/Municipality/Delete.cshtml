@using VotingWeb.Properties
@using VotingWeb.Areas.Admin.Models.Municipalities
@model DeleteModel
@{
    ViewBag.Title = MunicipalityAdminRes.PAGE_TITLE;
}

<div id="content">
    <div class="float-right">
        @Html.ActionLink(AdminRes.BUTTON_LIST, "Index", "Municipality", null, new { @class = "btn btn-info" })
    </div>
    <h1>@MunicipalityAdminRes.PAGE_TITLE_DELETE</h1>
    @using (Html.BeginForm())
    {
        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.Name)
        <div class="form-group">
            @Html.LabelFor(model => model.Name)
            <div class="form-control">@Model.Name</div>
        </div>
        <div class="btn-group" role="group" aria-label="...">
            <button type="submit" class="btn btn-warning">@AdminRes.BUTTON_DELETE</button>
        </div>
    }
</div>
